{
  "name": "ninja-prep",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "test": "jest",
    "start": "node server/server.js",
    "heroku-postbuild": "YARN_CONFIG_PRODUCTION=true yarn --cwd ./server/ install && yarn --cwd ./client/ install && yarn --cwd ./client/ build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/core": "^7.11.6",
    "@babel/plugin-transform-modules-commonjs": "^7.10.4",
    "codecov": "^3.7.0",
    "jest": "^26.4.2",
    "prom-client": "^12.0.0",
    "yarn-recursive": "^1.1.0"
  },
  "jest": {
    "coverageDirectory": "./coverage/",
    "collectCoverage": true
  },
  "devDependencies": {
    "@babel/plugin-syntax-jsx": "^7.10.4",
    "@babel/preset-react": "^7.10.4",
    "prettier": "^2.0.5"
  }
}
