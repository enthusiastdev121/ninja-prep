import {ProblemSubmissionInput} from '@ninjaprep/types/ProblemSubmission';

class DockerService {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  run(input: ProblemSubmissionInput): void {
    return;
  }
}

export default DockerService;
